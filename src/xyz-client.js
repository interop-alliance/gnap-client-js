'use strict'

export class XyzClient {
  constructor ({ store, clientDisplay, capabilities }) {
    this.store = store
    this.clientDisplay = clientDisplay
    this.capabilities = capabilities
  }
}
